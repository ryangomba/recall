import SwiftUI

struct ShareView: View {
    var body: some View {
        Text("Saved as draft")
    }
}

struct ShareView_Previews: PreviewProvider {
    static var previews: some View {
        ShareView()
    }
}
